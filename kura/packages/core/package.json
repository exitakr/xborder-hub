{
  "name": "@kura/core",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "//": "Source-only package: no build step. The web app transpiles it via next.config transpilePackages, and the mobile app via Metro's monorepo watchFolders. One less artefact to keep in sync.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types \"src/**/*.test.ts\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.46.1",
    "typescript": "^5.7.2"
  }
}
